# Memory Workspace Fixture

The project prefers local-first operation, auditable config changes, and eval-gated promotion of durable improvements.

Transient observations should stay in trace or artifacts. Durable memory should be scoped, sourced, non-secret, and likely to help future runs.
